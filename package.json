{
  "name": "truckload-splitter",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"   // optional: for local development
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"       // remove if using Node 18+ native fetch
  },
  "engines": {
    "node": ">=18"               // ensures Render uses Node 18 or newer
  }
}
